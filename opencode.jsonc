{
  "$schema": "https://opencode.ai/config.json",
  // Loads the Flight Deck TUI plugin from this checkout.
  //
  // Its appearance is configured in flight-deck.jsonc (or
  // .opencode/flight-deck.jsonc) — copy flight-deck.example.jsonc to get
  // started. Options passed here also work on hosts that forward them.
  "plugins": ["."]
}
